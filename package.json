{
  "author": "Tom Carden",
  "name": "csv2html.js",
  "description": "Convert a CSV file into an HTML file using a simple template.",
  "version": "0.0.0",
  "repository": {
    "url": ""
  },
  "main": "csv2html.js",
  "scripts": {
    "build": "node csv2html.js data/input.csv templates/template.ejs build/output.html",
    "prebuild": "del-cli --force build && mkdir build" 
  },
  "dependencies": {
    "csv": "0.0.10",
    "ejs": "0.6.1"
  },
  "devDependencies": {
    "del-cli": "^4.0.1"
  }
}
